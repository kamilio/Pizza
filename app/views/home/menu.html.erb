<h1>Menu</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Price</th>
  </tr>

<% @items.each do |item| %>
  <tr>
    <td><%= link_to item.name, item %></td>
    <td><%= item.description %></td>
    <td><%= item.price %></td>
  </tr>
<% end %>
</table>