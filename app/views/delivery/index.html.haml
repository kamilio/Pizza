%h1 Deliveries
%table{:width => 500}
  - @orders.each do |order|
    %tr        
      %td
        %strong= order.user.name
        %br
        = nl2br(order.user.address).html_safe
      %td
        =number_to_currency(order.sum)
      %td        
        =link_to "Delivered", delivery_path(order), :method => "delete", :class => "btn"